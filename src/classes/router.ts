// import { IRouter } from './../interfaces/iRouter';
// import { IRouterMatcher, PathParams, RequestHandler } from 'express-serve-static-core'; 
// export class Router implements IRouter {
//     public timeEntry = router(path: PathParams, (request: any, response: any) => {

//     });

// }